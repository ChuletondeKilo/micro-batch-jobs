[project]
name = "job-dispatcher"
version = "0.1.0"
description = "Service to dispatch jobs"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.34.0",
    "requests>=2.31.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
