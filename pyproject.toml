[project]
name = "micro-batch-jobs-workspace"
version = "0.1.0"
description = "Monorepo workspace for micro-batch-jobs"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = ["job-generator", "job-dispatcher", "worker"]
